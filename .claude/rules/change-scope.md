# Change Scope

- Read the relevant code before editing.
- Prefer editing existing files over creating new ones unless a new file is clearly justified.
- Keep changes inside the requested scope.
- Avoid speculative refactors, abstractions, or extra configurability unless the task requires them.
- Prefer the smallest correct change that matches local conventions.
- If the work becomes non-trivial, run a verification pass before reporting completion.
